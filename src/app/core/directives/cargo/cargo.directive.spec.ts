import { CargoDirective } from './cargo.directive';

describe('CargoDirective', () => {
  it('should create an instance', () => {
    const directive = new CargoDirective();
    expect(directive).toBeTruthy();
  });
});
